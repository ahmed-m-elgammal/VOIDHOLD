# T0.8 Output — Parity Fixture Starter (18 buildings, canonical IDs)

Reference: original JS elapsed-time ~50ms cadence (not fixed tick). Godot target: fixed 20Hz sim + render interpolation + 4h offline fast-forward (Epic 1).

## Canonical resources
`stone, alloy, biomass, ore, stim`. Legacy: `money->alloy, nutrition->biomass, drugs->stim`.

## Fixture columns (per building)
`type | stone | alloy_cost | energyNeed | maxWorkers | updateDelta_ms | input{res,perWorker} | output{res,perWorker,max} | updateDamage | constantDamage | planetProperty | perWorker{type,radius} | mood_effects | storage_caps | rounding/clamp notes | intentional_fix | mobile_override`

## 18 rows to fill (verbatim first)
elevator, storage, living, quarter, mine, mineshaft, oremine, farm, farmarea, factory, kitchen, cantine, bar, maintenance, authority, solar, oxygen, teleport

## Cases per system
Normal, empty, full, damaged>0.2/>0.9, destroyed=1.0, no-energy, no-input, starving (spawn needs happiness>0.8 AND fullness>0.8), interrupted (save mid-route/attack/repair), multi-dome teleport (5 units/60s neediest-first), enemy spawn 20s 50% with INTENTIONAL FIX cap 3 (source allowed 4), authority 0.2 per 2000ms action (= per 40 steps at 20Hz) within 80px.

## Rules
Parity vs `intentional_fix` vs `mobile_override` labeled separately. Full RNG state persisted and replayed (seed alone insufficient), tie-breaks + empty-list behavior recorded. No TBD in sim sections for QA sign.

## Accept
Tables cover all cases, 18/18 rows + readability reqs, QA signs parity-vs-change boundary. Feeds T1.6 golden tests.
