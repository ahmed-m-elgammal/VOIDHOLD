# T0.1 — Rebrand + Identity + Store Setup (Docs only) — AUTHORITATIVE TASK SPEC
> Authority: this file is the single source of truth for T0.1. `T0.1-output-decision-draft.md` is a supporting ARTIFACT, not authoritative.

Owner: Producer. Reviewer: Designer.
Inputs: working title `VOIDHOLD: Moon Colony`.
Output: name lock, logo brief, bundle IDs, store reservations, privacy URLs.

## 1. Tasks
- [ ] Decision approved: `VOIDHOLD: Moon Colony` (STARHOLD retired). Verify before store setup: Play exact match, App Store exact match, domain/social handle, trademark USPTO/EUIPO quick search — all unchecked, store setup BLOCKED until checked.
- [ ] Lock subtitle `Moon Colony` + short description (80 chars) + keywords ASO draft (do not publish yet).
- [ ] Lock bundle IDs: `com.voidhold.colony` Android + `com.voidhold.colony` iOS. Reserve in Play Console + App Store Connect (draft app, no upload) ONLY after checks above pass.
- [ ] Logo brief: isometric dome mark, readable at 48dp, light/dark + monochrome versions. Neon cyan + ice-blue + warm orange per art pillars.
- [ ] Privacy policy + support URL placeholders (required for API 36 + iOS submission later).
- [ ] Rename map: old `Aussenposten/Hedon/mighty/helot/drugs/money` -> new player-facing terms. Keep internal IDs stable to avoid port churn. Proposal:
  - `money -> Alloy`, `nutrition -> Biomass`, `drugs -> Stim`, `helot -> Crew`, `mighty -> Officers`, `Hedon -> Hedon Prime`
  - Decision log required if deviating.

## Canonical ID decision

- [ ] Canonical resource IDs are locked as `stone`, `alloy`, `biomass`, `ore`, and `stim`.
- [ ] Import mapping is recorded as legacy `money -> alloy`, `nutrition -> biomass`, and `drugs -> stim`.
- [ ] Internal building/caste IDs remain stable even when display names change (`cantine`, `oppulence`, and other legacy spellings included).
- [ ] Display strings never become save keys.

## 2. DB / data impact (plan only)
- No DB migration yet. Record `string_id` policy: internal IDs immutable (`mine`, `oremine`), display names localizable via `strings.csv`. This prevents save-breaks on rename.

## 3. UI/UX impact
- Logo must work in splash, app icon (adaptive Android + iOS 1024), top-HUD dome badge, shop header. Provide safe-area + small-size legibility test.

## 4. Best practices
- Searchable, spellable, <12 chars base name. Avoid `Outpost` saturation — check top 50 builder results.
- Bundle ID never changes after first upload. Triple-check before reserving.
- Keep original `Aussenposten` credit line in About for goodwill, not as title.

## 5. Accept
- [ ] Name decision signed + reservation screenshots linked
- [ ] Bundle IDs doc + privacy/support URLs
- [ ] Logo brief + rename map approved
- [ ] Canonical ID and legacy mapping approved
- [ ] No code committed for this task
