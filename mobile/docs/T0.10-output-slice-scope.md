# T0.10 ARTIFACT — VOIDHOLD Premium Slice Scope (supporting output, NOT authoritative)
> Authoritative spec: `T0.10-premium-vertical-slice.md`.

Game: `VOIDHOLD: Moon Colony` (`com.voidhold.colony`). Locales v1: `en` + `ar` (RTL). Engine 4.7.2 Mobile.

## Slice (one dome, one moon — exact chain)
Oxygen + storage + habitat(living) + farm + farmarea(greenhouse) + cantine (food delivery loop: grow->process->store->eat) + mine + mineshaft (stone loop) + solar + maintenance (damage/repair loop) + authority + 1 flyer siege loop. Both castes (Crew/Officers) + carrierbot fetch/store. Placement + assignment + damage/repair + reports entry + tutorial entry + save/load/resume + 2x + offline fast-forward check. Storage, canteen, and full resource logistics must be demonstrated end-to-end, not just production alone.

## Production-quality subset
Environment kit (snow/rock/ice), 4+ finished PBR buildings (oxygen/habitat/greenhouse/solar), final lighting, final HUD/sheet direction, final SFX language, portrait + landscape + ar RTL.

## Gate (all must pass, else pause bulk production)
- [ ] First useful action <5min, no external instructions, en + ar
- [ ] Touch place/select/camera/sheets/back/orientation deliberate, 48dp, safe-area
- [ ] Save survives interrupt, low-storage sim, kill-during-write, resume; replay deterministic
- [ ] Sim/view synced after pause/speed/offline; no dangling refs
- [ ] Perf within `mobile/device-matrix.md` numerical budgets (p95, RAM, thermal, battery, startup, save, 4h fast-forward) on required physical Low + iPhone 11
- [ ] Readability: 5-user panel (incl. 1 color-blind, 1 ar RTL) identifies every building/unit/warning at low/med/high zoom on min device; visual lead sign + screenshots attached. Generic/repetitive/unreadable/placeholder fails.

IAP: skipped — shop stays Locked, EntitlementManager stub only (Epic 8 deferred).
