# T0.3 ARTIFACT — License Audit Starter (supporting output, NOT authoritative)
> Authoritative spec: `T0.3-license-purge.md`.

## Default decision: REPLACE (quarantine until CC0 proof)
- `imagesoga/lpc-skorpios-scifi-sprite-pack/*` — REPLACE (new PBR buildings/props)
- `imagesoga/lpc-tile-atlas2/*` — REPLACE (ground shader, no tiles ship)
- `imagesoga/tiled-terrains/terrain.png` — REPLACE (baked masks only, no pixels ship)
- `imagesoga/1-hour-lpc-enemy/*`, `imagesoga/lpc-beetle/*` — REPLACE (new flyer rig)
- `images/bodybuilder/*` (LPC bodies/hair/facial/torso/pants/feet/heads per bodybuilder.js) — REPLACE (original 3D customization + portraits)
- `music/*.ogg` (11 tracks) — REPLACE (6 new adaptive tracks, -14 LUFS)
- `sounds/*` — REPLACE or re-record with proof
- `maps/map.tmx` tileset PNGs — logic kept, visuals replaced
- `lib/gamejs*, jquery*, mustache*, raphael*` — runtime only, do not ship; document only. Fonts — verify OFL/CC0 per file.

## art_manifest.csv format
`new_guid,old_path,source_url,author,license,commercial_ok,attribution_text,decision,notes`

## Outputs
- Audit sheet filled per row, `CREDITS.md` + `THIRD_PARTY_NOTICES.md` generated from approved rows.
- `art_final/` accepts only approved + provenance-attached. `art_inbox/` quarantine otherwise. Release export fails on unapproved path.

## Accept
Zero ship-blockers, replacement list costed, T0.7 review signed. No public build until passed.
IAP: skipped — Epic 8 stub only, no SDK.
