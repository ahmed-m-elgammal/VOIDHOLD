# T0.1 ARTIFACT — Name + Identity Decision (supporting output, NOT authoritative)
> Authoritative spec: `T0.1-rebrand-identity.md`. This file records the decision draft only.

## Decision (APPROVED-PENDING-VERIFICATION — store setup BLOCKED until checks pass)
- Game: `VOIDHOLD` — subtitle `Moon Colony` — full title `VOIDHOLD: Moon Colony`
- Bundle: `com.voidhold.colony` (Android + iOS, never changes after first upload)
- Status: decision approved, NOT locked for store. Unblocks T0.2 wording + T0.5 strings only. Store reservation stays blocked.

Checks required before lock (owner: producer):
- [ ] Play exact-match search top 50 builder results
- [ ] App Store exact-match + subtitle availability
- [ ] Domain/social handle quick check
- [ ] USPTO/EUIPO quick search screenshot

## Bundle IDs (locked, do not upload yet)
- Android: `com.voidhold.colony`
- iOS: `com.voidhold.colony`. Reserve as draft app only after store checks below.

## Localization scope (locked)
- v1 launch: `en` + `ar` only (English LTR + Arabic RTL).
- Later: `es` (Spain Spanish) + `de` (German). No code changes needed — strings + fonts + layout only.
- Rules: internal IDs immutable, display via `strings.csv` per locale (`strings_en.csv`, `strings_ar.csv` reserved, `strings_es.csv`/`strings_de.csv` stubbed later). Arabic requires RTL mirroring, font fallback, number formatting, and TalkBack/VoiceOver verification in Epic 6.

## Rename map (internal IDs stable)
Display only, saves keep internal IDs:
- `money -> Alloy (alloy)`, `nutrition -> Biomass (biomass)`, `drugs -> Stim (stim)`
- `helot -> Crew (crew)`, `mighty -> Officers (officer)`
- `Hedon -> Hedon Prime`, `cantine` spelling kept internally, display `Canteen`
- Legacy spellings (`oppulence`, `cantine`) stay as IDs with comment.

## Canonical IDs (locked for T0.6/T1.2)
`stone, alloy, biomass, ore, stim`. Legacy import: `money->alloy, nutrition->biomass, drugs->stim`.

## Next
T0.1 signed (name + bundle + rename map + en/ar scope). Unblocks T0.2 GDD wording + T0.5 strings_en/ar + Epic 6 RTL pass.
